#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_build:
	qmake
	$(MAKE)

override_dh_auto_install:
	$(MAKE) INSTALL_ROOT=`pwd`/debian/redis-desktop-manager install
	mkdir -p `pwd`/debian/redis-desktop-manager/usr/bin/
